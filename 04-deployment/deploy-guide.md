# Guide de déploiement (Oracle Linux ARM)

1. Installer Docker + Compose
2. Créer `.env` (prod)
3. Lancer `docker compose up -d`
4. Vérifier Traefik + certificats
5. Vérifier worker (logs) et génération PDF
